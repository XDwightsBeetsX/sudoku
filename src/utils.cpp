
#include <vector>
#include <iostream>

using namespace std;

vector<string> split(string inputString, string delimiter) {
    vector<string> result;
    bool done = false;
    while (!done) {
        int start = 0;
        int end = inputString.length();
        int i = inputString.find(delimiter);
        if (i == -1) {
            
        }
        result.push_back(inputString.substr())
    }
}
